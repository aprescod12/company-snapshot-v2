# Company Snapshot V2

A restart of the Company Snapshot take-home build, optimized for the smallest reliable implementation that actually satisfies the assessment.

## Product

A user enters a company name or website and receives:

- a clear 2–3 sentence description of what the company does;
- 3 recent, real company signals;
- source links where possible;
- a clean one-page experience with an honest loading state.

All production company information must come from the live public web. No mock or hardcoded company facts.

## Status

**Phase C1 is reviewed, pushed, remotely confirmed, and formally approved** at `e6ce58e91cf7806c6096ca904762218855e63c1c`. `api/snapshot.mjs` is a native Vercel Web-standard POST endpoint over the frozen `createCompanySnapshot()` entry point. It reads `EXA_API_KEY` only from the server environment, invokes B5 once for each valid submission, returns B5's four public states without rewriting source URLs, and sanitizes invalid requests, missing configuration, and unexpected failures. Its focused endpoint tests passed 9/9 and the complete zero-network suite passed 301/301 at C1 completion. **Phase C2 is reviewed, pushed, remotely confirmed, visually reviewed, and formally approved** at `8a31ad18824b54a1b0a7c59d5e0e5a785ae93f10` (`PHASE C2 — APPROVED AND CLOSED`); its focused browser tests passed 36/36 and the complete zero-network suite passed 337/337. **Phase C3 then added a zero-provider-network local integration harness, `scripts/c3-local-ui-harness.mjs`, that serves the real `public/` frontend and a synthetic-fixture `/api/snapshot` route so the browser → fetch → render path can be exercised end to end without a live provider, with no deployment, live endpoint, or B1–B5/C1/C2 change; it is reviewed, pushed, remotely confirmed, and formally approved and closed** at `8f2d13ceb0e9ddbf13b6750f7163faf78ad6ca78`. Cumulative Exa accounting after C3 remained Search 42 / Contents 9 / retries 0.

**Phase D1 then publicly deployed the exact approved commit** (`8f2d13ceb0e9ddbf13b6750f7163faf78ad6ca78`) to Vercel via CLI from a clean git worktree, at **https://company-snapshot-v2.vercel.app** (project `amiri-prescods-projects/company-snapshot-v2`, zero-config static + `api/snapshot` function, `EXA_API_KEY` configured Production-only as a hidden Secret). Static/HTTP preflight passed fully; the two D1-authorized live submissions (NVIDIA, stripe.com) returned `clarification_needed`/`company_ambiguous` and `unavailable`/`provider_unavailable` respectively — both safe, documented public-contract outcomes with no deployment/runtime defect observed, but neither reached `snapshot`. **Phase D2 then validated the deployed application against a bounded live cohort** (`notion.so`, PostHog, Datadog, Mercury, plus one owner-approved additional `notion.so` browser re-submission for visual confirmation): of the 4 original cohort companies, 3 reached `snapshot` (`notion.so`, PostHog, Mercury) with correct identity, factually accurate 2–3 sentence descriptions, and exactly 3 signals each, while Datadog reached a safe `unavailable`/`description_unavailable`; the additional owner-approved `notion.so` browser re-submission also reached `snapshot` — so 4 of the 5 total submissions reached `snapshot`, representing 3 unique companies (`notion.so` was submitted twice). All 9 signal source URLs across the 3 snapshots were manually opened and independently re-verified (material event support, source-derived date consistency, and company identity all confirmed — displayed signal wording was supported by the relevant publisher headline/content, not necessarily an exact HTML `<title>` match), all three signal sets were confirmed distinct (no duplicate-event coverage), and recency-bucket assignments were confirmed correct. The owner independently confirmed one rendered snapshot in-browser, including the loading state. No B1–B5, C1, C2, or C3 file changed during D1 or D2. See `docs/PHASE_D2_PRODUCTION_VALIDATION.md` for the full record. Cumulative Exa accounting after D1+D2 is a defensible range of Search 48–54 / Contents 14 / retries 0 (exact per-call Search/fallback counts are not independently observable from outside the deployed function). The full final assessment testing matrix has not begun.

**Phase A is `PHASE A ARCHITECTURE APPROVED FOR IMPLEMENTATION`: one broad Exa discovery pass, bounded verification, and at most one official-domain fallback. B1's deterministic company-targeting boundary is complete and its live gate passed. B2 is `B2 PRODUCTION BROAD DISCOVERY — APPROVED`. B3 is `B3 PRODUCTION EVIDENCE VERIFICATION — APPROVED` after its B3R2 owner-run NVIDIA replay reached `verified` and a publisher-only validation confirmed the extraction/date correction. B4A is a pure, zero-network deterministic snapshot assembler over completed B3 output, approved, with no B1–B3 change. B4B is `B4B ISOLATED GROUNDED COMPANY DESCRIPTION — APPROVED` after its two owner-authorized NVIDIA and stripe.com Contents live gates each passed manual review, with no retry and no change to B1–B4A. B5 Stage A adds a zero-network production orchestration boundary over the approved B1–B4B exports; it is implemented and zero-network tested. A first live-gate CLI attempt stopped at credential preflight (0 network-backed executions); a second, network-backed NVIDIA attempt reached the real pipeline but B4B rejected the description (`unavailable`/`description_unavailable`), with the underlying reason lost to a since-fixed harness gap. A third, network-backed NVIDIA attempt then **passed**: the full B1→B4A path succeeded end-to-end (`state: "snapshot"`, resolved `NVIDIA Corporation`/`nvidia.com`, 3 verified signals, 1 Search, 1 Contents, no fallback, no retry). B5 is `B5 INTEGRATED LIVE GATE — PASSED FOR ONE KNOWN-GOOD COMPANY (NVIDIA)`. The broader Phase B validation gate then ran the six-case representative cohort once each (Stripe, PostHog, Canva, notion.so, Mercury, Craigslist): three reached `snapshot` with 3 verified signals; three reached `clarification_needed` (Mercury as the intended safety outcome; notion.so and Craigslist as open findings). A subsequent project-owner review found the original "COMPLETE" framing understated the actual gate status: manual grounding continuation (0 Exa calls) opened 5 of the 9 recorded signal sources and confirmed they materially support their claim with a matching date — the other 4 (1 PostHog, all 3 Canva) had no exact URL recoverable from the persisted record and were not guessed or fetched; Canva's signals 2/3 duplicate-event question remains **unconfirmed** by page inspection, not established as a duplicate; a validation-harness-only fix now preserves the real underlying B1/B2 discovery reason instead of the public contract's collapsed `company_ambiguous`; and two authorized diagnostic reruns found `notion.so`'s underlying reason is `contradictory_identity` and Craigslist's is `insufficient_identity_evidence`. That was `PHASE B VALIDATION — NOT APPROVED / REQUIRES PROJECT-OWNER DECISION`; no B1–B5 production `src/` file had changed, and no safety or fabrication failure was found. **B1R1** then implemented a project-owner-approved, generic, conservative cross-TLD canonical-domain reconciliation rule in B1 targeting that fixes `notion.so`'s specific `contradictory_identity` root cause (full detail: `docs/PHASE_B1R1_RECONCILIATION.md`); the one authorized live confirmation showed `notion.so` now correctly resolves to `Notion Labs, Inc.`/`notion.com`, but the same run separately hit `insufficient_evidence` in B3 verification — a new, distinct downstream finding. A B3R0 diagnostic then isolated the exact cause to B3's `matchesCompany()` gate: the resolved legal name `"Notion Labs, Inc."` requires the literal word "Labs" to appear in article text, which ordinary first-party Notion product/blog copy never contains. **B3R3** added one narrow, `FIRST_PARTY`-only brand-anchor fallback in `src/verification/verifyCompany.mjs` — used only after the existing strict full-name match fails, only for sources already classified `FIRST_PARTY` against the B1/B1R1-confirmed official domain, requiring a domain-derived brand token to be consistent with the resolved name AND literally present in the article text — with the pre-existing strict path completely unchanged for every other source class and company. The one authorized live confirmation then reached a full `snapshot`: `notion.so` resolved to `Notion Labs, Inc.`/`notion.com` with a grounded description and 3 manually-verified, distinct, dated, first-party signals. Endpoint/UI wiring and deployment have not begun. Cumulative Exa Search requests were 39; Exa Contents requests were 9; retries were 0 at that point. Two further zero-repair identity diagnostics followed (Craigslist: reproduced `Craigslist, Inc.` / `craigslist.org` / `ambiguous:false` with no grounding citation on `craigslist.org`, classified a safe **provider-evidence limitation**, no repair; Canva: three different live outcomes across three separate attempts — a historical `snapshot` with an unresolved duplicate-event question, a later `insufficient_identity_evidence` failure, and a fresh resolved pass with one on-domain grounding citation — classified as **provider-response variability**, not a deterministic bug; the historical Canva duplicate-event concern remains **UNRESOLVED**, not confirmed). The project owner then approved **`PHASE B — APPROVED FOR FRONTEND INTEGRATION WITH KNOWN LIMITATIONS`**, explicitly *without* retroactively marking `docs/TESTING.md` §3.2's 5/5 ordinary-resolution gate as passed — that gate remains not repeatably demonstrated, an accepted internal-benchmark shortfall rather than a corrected defect, because the observed Canva failure is safe provider variability rather than wrong-company resolution, further Search repetitions would only measure stochastic provider behavior, and speculative fallback logic would add complexity without direct supporting evidence. Cumulative Exa Search is now 42; Contents remains 9; retries remain 0. B1–B5 backend architecture is now **frozen for Phase C frontend integration**, absent a concrete correctness/security defect found during later end-to-end testing. See `docs/PHASE_B_OWNER_DECISION.md`. A4.3 remains `A4.3 SIGNAL BENCHMARK FAIL`; A4.3R0 remains `NO POLICY CHANGE YET`; and A4.3R1 remains `RETRIEVAL REPAIR FAIL`.**

The approved Gemini 2.5 Flash hypothesis failed its one-request NVIDIA smoke because that model was unavailable to the new-user project. The later Exa NVIDIA smoke made exactly one request through the verified Free Tier/no-payment path. Exa returned a structurally valid snapshot and grounding, but manual review found an assessment-trivial repository change, a 239-day-old signal from a weak secondary source, and an acquisition claim whose generic source destination was inaccessible and did not materially support the event. The tested Exa `auto + outputSchema + output.grounding` final-snapshot hypothesis is therefore `NO-GO`.

Exactly one separately authorized raw `auto` Search request returned 10 highlighted candidates. Manual review found three distinct, first-party, materially supported NVIDIA events, all within 90 days. A4.1 demonstrated that a dedicated signal-oriented raw-discovery query can retrieve enough strong recent candidates for NVIDIA, supporting a decomposed discovery → selection direction. Because the A4.1 query differed from A3's final-snapshot query, the experiment does not isolate whether A3 failed solely in downstream selection versus retrieval-query formulation. A4.1 request count is 1, cumulative Exa request count is 2, no retry occurred, and the runtime architecture remains intentionally **not frozen**.

A4.2 now provides a generic dependency-free selector for already-retrieved candidates. Its one authorized NVIDIA smoke reused the A4.1 request contract and returned 10 candidates; the selector chose ranks `1,3,5`. Manual review found those three first-party selections supported, material, distinct, and within 90 days, so the smoke passed.

A4.3 reused that NVIDIA result and then ran the frozen discovery → selector pipeline sequentially on Stripe and PostHog. Each one-request raw set contained only two qualifying distinct events, so both cases were `DISCOVERY INSUFFICIENT`. After two new failures, even successful Canva and `notion.so` cases could reach only 3/5, making the required 4/5 impossible; the benchmark stopped without those requests. A4.3 made 2 requests with zero retries, cumulative Exa request count is 5, and the result returns for project-owner reassessment. The recurring lexical duplicate-recall misses did not corrupt any observed selected set.

A4.3R0 used no provider access and preserved that historical result. It found that PostHog's supported first-party rank 9 event, about 182 days old, would make the raw set sufficient under a bounded sparse fallback—but the frozen selector would still choose invalid rank 7 instead. Stripe's roughly 272-day-old rank 9 event was returned through a recent retrospective explainer and remains ineligible under defensible safeguards. Because neither broader window repairs the actual pipeline and the evidence is sensitive to cutoff choice, the reassessment recommends no change yet. A separate decision about evidence-aware source verification/backfill remains required.

A4.3R1 then made one new Stripe request with the A4.1 retrieval contract unchanged except for one fixed source-quality/novelty `systemPrompt`. Exact-destination review again found only two qualifying distinct ≤180-day events. The frozen selector selected `2,1,3`, including two rejected candidates, but raw insufficiency triggered the required early stop before PostHog. The repair is therefore not supported; A4.3R1 made 1 request with zero retries and cumulative Exa requests are 6.

A4.4 froze the smallest evidence-supported direction: Exa remains the primary discovery provider, but the one-search → selector → final-three shape is rejected. A4.5 then tested its only conditional branch with one Stripe official-domain fallback. It returned a distinct, supported August 19 OpenRouter acquisition beyond the preserved FX and Lloyds signals, so Phase A is approved for implementation. The ceiling remains two discovery requests per company; if the bounded path fails in a later appropriately authorized case, the product must return honest insufficient evidence rather than add layers.

B1 adds a dependency-free local boundary for user company targeting and later identity confirmation. It treats domain/URL input as a normalized hostname anchor, keeps name input provisional without guessing a domain, and keeps ordinary whitespace-containing punctuated company names on the name path. A name target needs compatible corroborated identity evidence plus explicit `ambiguous: false`; it does not require its official domain to resemble the company name. The first live gate revealed that Exa titles can be absent, so only title metadata became optional while valid HTTP(S) URLs remained required. Mercury then returned two entities with `ambiguous: true`, which B1 safely clarified. One authorized Stripe positive control preserved the frozen request/schema, returned `Stripe` / `stripe.com` / `ambiguous: false`, and B1 resolved it using exact first-party grounding. Mercury live requests are 2, Stripe is 1, cumulative Exa requests were 10 before B2.

B2 adds the minimal production broad-discovery boundary: one frozen broad Exa Search, B1 identity confirmation with exact field-specific grounding, normalized raw candidates, and the selector's complete `prioritized` verification queue (with `selected` retained as its first three). Its first live gate safely clarified; B2R0 isolated observability; B2R1 observed provider `ambiguous:true`; and B2R2.1 narrowed the approved legal-suffix identity exception. B2R3 then passed both authorized Stripe name and domain gates, so the project owner approved `B2 PRODUCTION BROAD DISCOVERY — APPROVED`. Cumulative Exa experimental requests are 14. See `docs/PHASE_B2_BROAD_DISCOVERY.md`.

B3 consumes B2's complete prioritized queue in order and verifies exact publisher URLs using bounded native GET retrieval: HTML/XHTML only, manual redirects capped at five, one five-second deadline across redirects, headers, and body, a 2 MiB body cap, and no cookies, browser automation, retry, crawl, or literal-IP target. It derives a publisher headline, compact supporting passage, and source publication date. After B3R1 isolated a legitimate NVIDIA Newsroom page shape that lacks `<article>`/`<main>`, B3R2 adds only a generic chrome-stripped static-body fallback and a tightly bounded visible near-headline date fallback; JSON-LD, article metadata, and `<time datetime>` retain priority, and provider dates remain ineligible. Stripe's owner-run B3R1 replay verified three broad first-party records; NVIDIA remained at two after broad plus fallback because legitimate pages failed the old structural gate. The three Stage B requests bring cumulative Exa to 21. The subsequently authorized B3R2 NVIDIA replay (one broad request, no fallback) reached `verified` with exactly three accepted records, bringing cumulative Exa to 22; a separate 0-Exa publisher-only check then confirmed the exact previously failing NVIDIA Newsroom page is now accepted with a publisher-derived date. The project owner approved `B3 PRODUCTION EVIDENCE VERIFICATION — APPROVED`. See `docs/PHASE_B3_LIVE_GATE.md`.

B4A adds `src/snapshot/assembleSnapshot.mjs`, a pure, dependency-free module that maps a completed B3 verification result plus a caller-supplied description into the assessment-shaped snapshot. It makes no network, Exa, publisher, or model call and does not import B1, B2, or B3; those remain a frozen compatibility boundary. Verified evidence (always three records) becomes exactly three signals in B3's existing order using the publisher-derived title/date/resolved URL, never the raw candidate values or internal fields like `evidenceSnippet`; insufficient evidence (0–2 records) stays partial with no padding.

B4B adds `src/description/exaCompanyDescription.mjs`, an isolated candidate for the description B4A intentionally does not generate. It is deliberately kept separate from B2's existing broad-discovery `outputSchema` so a description feature cannot risk the approved Search/identity/signal-retrieval behavior. It makes one Exa **Contents** request (never Search) against the exact HTTPS root of an already-confirmed official domain, requesting `summary` only, and validates the result structurally: 2–3 sentences, bounded length, no embedded URL, and a returned URL on the confirmed domain or a subdomain. It does not import B1, B2, B3, or B4A. The owner subsequently ran the two owner-authorized live gates: NVIDIA (`nvidia.com`) and Stripe (`stripe.com`) each made exactly one Contents request with no retry, returned a three-sentence description grounded in the official homepage, and passed manual review for accuracy, stable core-business focus, and absence of recent-news contamination. B4B Contents requests total 2 with 0 retries; cumulative Exa Search accounting remains 22, tracked separately. The project owner approved `B4B ISOLATED GROUNDED COMPANY DESCRIPTION — APPROVED`; two cases do not establish universal reliability, and broader end-to-end coverage remains a later concern. See `docs/PHASE_B4B_LIVE_GATE.md`.

B5 Stage A adds `src/orchestration/createCompanySnapshot.mjs`, a small dependency-free module that sequences the approved B1–B4B production exports (`discoverCompany` → `verifyCompanyDiscovery` → `requestCompanyDescription` → `assembleSnapshot`) into one application-facing result, without reimplementing or modifying any of them. B2 clarification stops immediately; both B3 outcomes (`verified` and `insufficient_evidence`) proceed to B4B using the confirmed identity, never raw input; B4A's result returns unchanged. The public contract is limited to `clarification_needed`, `snapshot`, `insufficient_evidence`, and a narrow `unavailable` state (`description_unavailable` or `provider_unavailable`) that never exposes API keys, provider messages, or internal diagnostics; an unexpected programming error is rethrown rather than absorbed. Testing uses one internal service-injection seam defaulting to the real exports, with no mocking library. B5 Stage A made 0 live network requests and is implemented and zero-network tested — mocked tests passing is not a live pass. The subsequently authorized one-shot NVIDIA integrated live gate (`scripts/phase-b5-live-smoke.mjs`) added 15 zero-network harness tests (all passing, including fake-network exercises of the real pipeline end-to-end and of the failure-diagnostic path) and then made one CLI live-gate attempt, which stopped at credential preflight — `EXA_API_KEY` was unavailable to the process, so zero network-backed B5 executions occurred (0 Exa Search/Contents/publisher requests) and no integration conclusion could be drawn. At that point, B5 remained **not live-gate approved**; a further NVIDIA live execution required fresh project-owner authorization once `EXA_API_KEY` was available. See `docs/PHASE_B5_LIVE_GATE.md`. Endpoint/UI wiring, deployment, and the frontend (Phase C) have not begun.

A second, network-backed NVIDIA attempt then reached the real pipeline: identity, verification (3 evidence, no fallback), and budget all behaved correctly, but B4B rejected the description, so B5 correctly returned `unavailable`/`description_unavailable` — though the harness's diagnostic omitted B4B's specific rejection `reason`, a gap fixed with 4 new zero-network regressions (that specific run's reason remains permanently unknown). A third, fresh-authorized NVIDIA attempt with the corrected harness then **passed**: `NVIDIA Corporation`/`nvidia.com` resolved, B3 verified 3 signals (2 `FIRST_PARTY`, 1 `OTHER`) with no fallback, B4B returned a grounded two-sentence description from `https://nvidia.com/`, and the final result was `state: "snapshot"` — 1 broad Search, 1 Contents request, 6 publisher requests, 0 retries. This is `B5 INTEGRATED LIVE GATE — PASSED FOR ONE KNOWN-GOOD COMPANY`; it does not establish reliability across arbitrary companies. See `docs/PHASE_B5_LIVE_GATE.md`.

The broader Phase B validation gate then ran the fixed six-case representative cohort (`scripts/phase-b-validation.mjs`, no `--company` argument — it always runs the fixed cohort in order; `runValidationCase()` itself also rejects any input outside that fixed cohort before making any network request) exactly once each: `Stripe`, `PostHog`, and `Canva` reached `snapshot` with 3 real, dated, sourced signals; `notion.so`, `Mercury`, and `Craigslist` reached `clarification_needed`/`company_ambiguous`. Mercury's clarification is the intended safety outcome (no wrong-entity resolution). No recency/dedupe/source/targeting/description rule was changed.

A subsequent project-owner review of this run corrected the record. **Manual grounding continuation** (0 Exa calls) opened 5 of the 9 recorded signal sources — 3 Stripe, 2 PostHog — reconstructed from exact title/date matches against other already-verified records in this repository, and confirmed each materially supports its claim with a matching publication date; the other 4 (1 PostHog signal, all 3 Canva signals) had no exact URL recoverable from the persisted record (only a truncated domain fragment survived) and were not guessed or fetched. **Canva's signals 2/3 duplicate-event question remains unconfirmed** by page inspection — title/metadata evidence is suggestive but not proof, and no dedupe change was made or implied. A **validation-harness-only observability fix** (no `src/` change) now preserves the real underlying B1/B2 discovery reason instead of the public contract's collapsed `company_ambiguous`, and two owner-authorized diagnostic reruns (1 Exa Search each, 0 retries) found the real reasons: `notion.so` → `contradictory_identity`; `Craigslist` → `insufficient_identity_evidence` — both identity-evidence-boundary outcomes, not the sparse-evidence outcome the Craigslist case was designed to test. Against `docs/TESTING.md` §3, this makes §3.2 (5/5 ordinary resolution) fail, §3.5 (manual grounding) only partially satisfied, §3.6 (distinctness) unconfirmed, and §3.8 (Craigslist honesty) not clearly satisfied. This was **`PHASE B VALIDATION — NOT APPROVED / REQUIRES PROJECT-OWNER DECISION`**, superseding the earlier "COMPLETE" framing; no B1–B5 production `src/` file changed at that point, and no safety or fabrication failure was found.

**B1R1** then implemented a project-owner-approved, generic, conservative cross-TLD canonical-domain reconciliation rule in `src/targeting/companyTarget.mjs`, fixing the specific root cause of `notion.so`'s `contradictory_identity` failure: a domain input may now accept a provider-proposed *different* top-level domain only when the provider is explicitly unambiguous, both hostnames are simple two-label domains sharing an exact (non-fuzzy) leftmost brand label, the resolved company name agrees with that label, the resolved-name grounding directly corroborates the proposed domain (at least one citation), and the official-domain grounding is exclusively on the proposed domain (every citation, not just one) — otherwise the existing `contradictory_identity` safety behavior is unchanged. This contains no company-specific code, no new dependency, and no new network path. Two independent sub-agent reviews found no false-positive exploit and no scope/safety issue (one structural, by-design residual risk was documented: very short/generic shared brand labels, e.g. `x.co`/`x.com`, cannot be distinguished from unrelated companies by this rule alone — an inherent limitation of domain-only input, not a bug). 30 focused targeting tests, 10 discovery tests, and 16 validation-harness tests pass (full suite 281/281); one authorized live run then confirmed `notion.so` now resolves correctly to `Notion Labs, Inc.` / `notion.com` (`ready_for_verification`, not `clarification_needed`) — but the same run's B3 verification separately reached `insufficient_evidence` (0 accepted records despite both Search requests and 20 publisher fetches), a new, distinct downstream finding this task does not investigate or repair. Cumulative Exa Search is now 35; Contents is 8; retries remain 0. **`PHASE B VALIDATION — NOT APPROVED / REQUIRES PROJECT-OWNER DECISION`** still stands — B1R1 fixes one identified root cause and surfaces a new one; it does not approve Phase B, and Craigslist/Canva/Phase C/UI/deployment remain untouched. See `docs/PHASE_B1R1_RECONCILIATION.md` and `docs/PHASE_B_VALIDATION.md`.

A B3R0 diagnostic then used the real B3 smoke observer against the resolved `Notion Labs, Inc.`/`notion.com` identity and found 19 of 20 attempted candidates rejected `unsupported_claim`, with every rejection's `sourceClass` populated but `recencyBucket` null — a signature that isolates the failure to B3's `matchesCompany()` gate before recency was ever evaluated. Three representative first-party `notion.com` pages, independently re-fetched with the unmodified production extraction code, confirmed real content, real dates, and the word "Notion" present but "Labs" absent — explaining the rejections: `matchesCompany()` requires every non-legal-suffix token of the resolved legal name (`"Notion Labs, Inc."` → `notion` + `labs`) to appear in the text, but ordinary first-party brand copy never says "Labs." **B3R3** then implemented one narrow, project-owner-approved fix in `src/verification/verifyCompany.mjs`: the existing strict full-name match is completely unchanged and still runs first for every source class; only when it fails, and only for a source already classified `FIRST_PARTY` against the B1/B1R1-confirmed official domain, may a second path succeed — requiring a domain-derived brand token (e.g. `notion.com` → `notion`) to be consistent with the resolved company name and to literally appear in the article text. Later gates (triviality, date, recency, support-sentence matching, duplicate) are completely unaffected either way. Two independent sub-agent reviews (a false-positive/safety challenge, and a full-diff review against 12 explicit non-goals) found no violation; one recommended test (a hyphenated multi-token brand label) was added, plus one explicit support-sentence-matching regression proving brand fallback passes company matching without bypassing that later gate. 11 new focused tests plus the full suite (292/292) passed before the one authorized live run, which reached a genuine `snapshot`: `Notion Labs, Inc.`/`notion.com`, a grounded 3-sentence description, and 3 manually-opened, dated, first-party, distinct signals (`notion.com/blog/introducing-developer-platform`, `notion.com/en-gb/releases/2026-09-09`, `notion.com/releases/2026-07-09`). Cumulative Exa Search is now 39; Contents is 9; retries remain 0. That was **`PHASE B VALIDATION — NOT APPROVED / REQUIRES PROJECT-OWNER DECISION`** — one case reaching `snapshot` does not re-certify the six-case cohort gates; Craigslist, Canva, and Phase C/UI/deployment remained untouched. See `docs/PHASE_B3R3_FIRST_PARTY_COMPANY_MATCHING.md`.

Two further zero-repair diagnostics closed out the remaining open findings. **Craigslist**: one fresh live Search reproduced `Craigslist, Inc.` / `craigslist.org` / `ambiguous:false`, but every citation in the combined identity-evidence grounding was a third-party commentary site (about Craigslist's business/ownership) — none on `craigslist.org` itself, even though a direct HTTP check confirmed `craigslist.org` is genuinely live. B1's refusal is exactly its designed fail-closed behavior given that evidence shape; the project owner classified this a **safe provider-evidence limitation / known limitation** — no repair authorized. **Canva**: three separate live attempts produced three different outcomes — the original cohort run reached `snapshot` with an unconfirmed duplicate-event concern between two near-identical Visual Suite titles; the later evidence-quality production run failed identity with `insufficient_identity_evidence`; and a fresh raw-identity diagnostic then resolved cleanly (`Canva` / `canva.com` / `ambiguous:false`, one on-domain grounding citation matching one of ten discovered candidates). The failed production run used no sub-agent and did not retain raw grounding detail, so its exact B1 predicate path was not independently reconstructed. The later raw identity diagnostic used one read-only B1 evidence-policy reviewer, which independently confirmed that successful B1 reconstruction. Across the observed runs, no deterministic B1 defect has been established. The project owner classified this **provider-response variability at the identity-grounding layer — no repair**; the historical Canva duplicate-event question remains **UNRESOLVED** (neither confirmed nor refuted, since the later runs never reached signal selection) and is deferred to later deployed/end-to-end validation rather than justifying speculative dedupe changes now.

Weighing this evidence, the project owner approved **`PHASE B — APPROVED FOR FRONTEND INTEGRATION WITH KNOWN LIMITATIONS`**. This is an explicit scope-and-priority judgment, not a claim that every internal gate passed: `docs/TESTING.md` §3.2's 5/5 ordinary-company-resolution benchmark is **not** marked passed and is preserved as an accepted, documented shortfall, because the observed Canva behavior is safe provider variability (never a wrong-entity resolution or fabricated evidence) rather than a fixable defect, because repeating Search calls would only sample stochastic provider behavior rather than isolate a deterministic fix, and because further backend hardening no longer materially improves the take-home relative to frontend, deployment, and production end-to-end testing. **B1–B5 backend architecture (targeting, discovery, identity policy, Exa query/schema, selector, verification, recency, date extraction, dedupe, source matching, description, orchestration, provider limits, retries) is now frozen for Phase C frontend integration**, to be revisited only if later deployed/end-to-end testing surfaces a concrete correctness or security defect. Cumulative Exa Search is now 42; Contents remains 9; retries remain 0. Full detail, rationale, and the complete owner-decision record: `docs/PHASE_B_OWNER_DECISION.md`.

Current provider decision sequence:

1. Gemini 2.5 Flash + Google Search grounding — `NO-GO` at A1 model access.
2. Exa `auto` Search with structured output and provider grounding — `NO-GO` at the NVIDIA A3 manual evidence gate; representative benchmark was not run.
3. Exa raw `auto` Search plus highlights — `DISCOVERY SUFFICIENT` at A4.1.
4. Deterministic recency/provenance/lexical-dedup selector — `SELECTOR PASS` on the single A4.2 NVIDIA smoke.
5. Fixed A4.1 discovery → A4.2 selector representative benchmark — `A4.3 SIGNAL BENCHMARK FAIL` after Stripe and PostHog were both discovery-insufficient; mandatory early stop at a 3/5 coverage ceiling.
6. Zero-provider A4.3R0 recency reassessment — `NO POLICY CHANGE YET`; broader windows do not repair selected-set validity.
7. Bounded A4.3R1 fixed-prompt retrieval repair — `RETRIEVAL REPAIR FAIL` at Stripe raw sufficiency; PostHog was not requested.
8. A4.4 architecture freeze — one broad Exa request plus at most one conditional official-domain fallback; selector is a prioritizer/deduper, not evidence approval.
9. A4.5 final one-request fallback feasibility — `FALLBACK FEASIBLE`; Phase A architecture approved for implementation. `deep-lite`, Tavily, and Groq are not automatic fallbacks.

See `docs/PLAN.md` for the complete current decision record.

## Governing documents

- `docs/ASSESSMENT_BRIEF.md` — authoritative product requirements
- `docs/WORKFLOW.md` — authoritative AI-assisted development procedure
- `docs/PLAN.md` — current approved V2 plan
- `docs/TESTING.md` — benchmark and verification gates
- `docs/AI_FAILURE_LOG.md` — genuine V2 failures only
- `AGENTS.md` — coding-agent operating instructions

## Development principles

- plan before implementation;
- use one bounded task at a time;
- benchmark real companies before architecture commitment;
- verify rather than trust agent completion claims;
- prefer the smallest architecture that works;
- correctness and coverage are evaluated together;
- every new layer must earn its existence through an observed failure;
- no paid APIs or paid keys;
- no scope expansion beyond the assessment without a concrete approved need.

## Repository

This is a clean V2 repository. V1 remains separate as historical research, empirical evidence, and failure analysis. V1 architecture should not be copied into V2 by default.

## Setup

The dependency-free browser UI is `public/index.html`, `public/styles.css`, and `public/app.js`. It sends one intentional `POST /api/snapshot` request with a JSON body such as `{ "input": "Stripe" }`; the endpoint is implemented in `api/snapshot.mjs` as a native Vercel Web handler and requires `EXA_API_KEY` only in the server environment. Never expose that variable to browser code or commit its value or a `.env` file. The application is publicly deployed at **https://company-snapshot-v2.vercel.app** (Vercel project `amiri-prescods-projects/company-snapshot-v2`, connected to this GitHub repository's `main` branch); `EXA_API_KEY` is configured there as a Production-only secret. The historical Phase A diagnostics require Node.js 22 and may be run only with explicit phase-specific authorization.

```bash
node scripts/gemini-phase-a.mjs smoke --company NVIDIA --confirmed-unbilled
node scripts/exa-phase-a.mjs smoke --company NVIDIA --confirmed-free-starter
node scripts/exa-phase-a-discovery.mjs discovery --company NVIDIA --confirmed-free-starter
node scripts/exa-phase-a-selector-smoke.mjs selector-smoke --company NVIDIA --confirmed-free-starter
node scripts/exa-phase-a-signal-benchmark.mjs --help
```

Do not run or rerun a live diagnostic without explicit phase-specific authorization. Each diagnostic makes at most one request and has no automatic retry or polling path. The A3 Exa diagnostic uses structured output plus grounding; the A4.1 diagnostic instead exposes raw result metadata and requested highlights for separate inspection without proving which A3 pipeline stage caused the failure. The A4.2 harness reuses that discovery request and hands candidates to the local selector in memory; the selector itself performs no network or source-page access. The A4.3 harness is a bounded historical benchmark tool, not an unrestricted production CLI; its live cases have already early-stopped and must not be rerun without new authorization. `deep-lite` has never been tested and is only a possible separately authorized fallback, never an automatic one.

## Submission targets

Before completion the repo should support:

1. live public URL;
2. plan/spec documentation;
3. preserved chat transcript or screen recording;
4. concise README with final setup/architecture/limitations;
5. testing record;
6. genuine AI failure log;
7. half-page reflection on where AI got stuck or was wrong and how those issues were detected and corrected.
