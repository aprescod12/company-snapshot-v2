# Company Snapshot V2 — AI Failure Log

## Purpose

Record only **genuine V2 AI mistakes, dead ends, incorrect assumptions, or implementation failures** that materially affect the work.

Do not manufacture failures for the final reflection.

V1 failures may be referenced in `docs/PLAN.md` as historical lessons, but they do not belong here as if they occurred in V2.

## Format

| Problem | AI mistake | How detected | What changed | Lesson learned |
| --- | --- | --- | --- | --- |

## V2 entries

| Problem | AI mistake | How detected | What changed | Lesson learned |
| --- | --- | --- | --- | --- |
| An untracked local credential was exposed in diagnostic command output. | The orchestrator ran a repository-wide secret-search command that printed matching lines. An untracked `.env` had appeared after the initial clean preflight, so its assignment was emitted even though the scan was intended only to verify hygiene. | The command output visibly contained the `.env` assignment. No provider request had been made. | The credential was not used and was later revoked/rotated and replaced; `.env`/`.env.*` are now ignored; the diagnostic redacts both recognized key formats plus the active key; remaining scans are limited to staged/tracked content and report only pass/fail, never matching lines. | A secret-hygiene check must never print candidate matches. Check known paths and staged/tracked blobs silently, especially because workspace state can change during a task. |
| The first Phase A diagnostic chose the legacy Generate Content API even though the recommended Interactions API met the required contract. | The implementation treated familiar `groundingMetadata` and `searchEntryPoint` field names as necessary, rather than checking whether the required Search execution, Suggestions display, direct result, and citation mapping behavior existed on the recommended surface. | Project-owner pre-push review prompted a fresh check of the current official Interactions overview, Search guide, API reference, and OpenAPI contract. | Before any provider request or push, the diagnostic was migrated to one Interactions call with `store: false`, matched Search call/result validation, array-shaped Suggestions handling, and inline URL-citation byte-span validation; mocked contract checks were expanded. | Select provider APIs by the required behavior and verify the recommended surface first; do not mistake one response schema's familiar fields for the product contract. |
| Phase A was designed around Gemini 2.5 Flash, but the new project could not access it. | During planning, AI verified capability/pricing documentation but did not identify the new-project model-access restriction before provider-specific diagnostic work was built. | The first real NVIDIA smoke returned `model_unavailable`; provider research then confirmed that documented model capability did not imply availability to the new project. | Gemini 2.5 Flash was rejected before A2, and account-level model availability is now treated as something that must be established as early as possible. | Provider capability/pricing documentation is not sufficient evidence of account-level availability. Prove the actual account/model path with the smallest possible smoke before investing in provider-specific architecture. |
| Human pre-push review challenged a correctly verified Exa Starter rate limit. | ChatGPT stated that current Exa pricing showed Starter at 5 Search QPS and Developer at 10, and instructed Codex to overwrite the repository's verified 10-QPS Starter value. | Codex rechecked the canonical Exa pricing page before making changes, found Starter at 10 Search QPS and Developer at up to 25, and stopped rather than following the conflicting instruction. | The erroneous correction was withdrawn. The repository retains the current canonical 10-QPS Starter value, and provider-source verification takes precedence over unsupported AI review claims. | A review instruction is not authoritative merely because it comes later. When it conflicts with a current canonical provider source, stop and verify rather than blindly applying the requested change. |
| A4.1 was intended to distinguish retrieval failure from downstream selection failure. | The experiment changed both the retrieval query and the removal of final-snapshot synthesis, introducing a confound and making the causal attribution too strong. | Post-experiment review compared the actual A3 and A4.1 request contracts. | `DISCOVERY SUFFICIENT` remains unchanged, but documentation now limits the conclusion to viability of the A4.1 signal-oriented discovery strategy. | When attributing failure to one pipeline stage, hold upstream inputs constant; otherwise treat the result as viability evidence rather than strict causal isolation. |
| Human review was checking whether the tightened 0.70 dedup threshold still preserved a realistic same-event wording variant. | ChatGPT incorrectly claimed `release` was in the selector stopword set and predicted that `release` versus `released` would produce Jaccard 0.80 and continue clustering. In the implementation, `release` is a metadata-title token, not a stopword, so both variants remained distinctive and the actual Jaccard score was about 0.667. | Codex restored the stronger fixture exactly as instructed and ran the focused and full test suites. The focused suite failed 21/22, and direct token-pipeline inspection established the actual 4/6 union calculation. | The 0.70 precision threshold remains intact. Instead of weakening the test or threshold, the selector now applies a small generic morphological normalization so simple variants such as `released` and `release` normalize consistently. | Do not infer tokenization behavior from memory or nearby constant names. Before predicting threshold behavior, inspect the actual normalization and stopword pipeline or let an executable test establish it. |
| Malformed provider dates received valid recency buckets. | The selector relied on permissive `Date.parse` behavior despite documenting malformed dates as `UNKNOWN`. | Independent pre-live review plus executable reproduction showed `2026-02-30` classified as `OLD` and `2026-09-08junk` as `FALLBACK`. | Permissive parsing was replaced with strict syntax and calendar validation, with impossible-date, trailing-garbage, leap-year, and valid-provider-format regressions. | Correctness-critical provider metadata needs contract-level validation, not merely parse-success checks. |
| The recency reassessment risked reverse-fitting a wider cutoff to failed benchmark captures. | The initial draft recommended a nine-calendar-month sparse fallback after over-weighting PostHog's strong 182-day candidate, even though it also established that broader recency repaired neither frozen selected set and made Stripe sensitive to a one-to-two-day boundary plus retrospective evidence. | A bounded independent review compared raw sufficiency with actual selection, highlighted that 270 days is a 50% expansion based on one boundary case, and challenged whether the cutoff was being chosen to improve the benchmark. | Before commit, the recommendation changed to `NO POLICY CHANGE YET`; the existing 90/180 testing contract and all executable behavior remain unchanged. | Evaluate policy changes against end-to-end behavior and cutoff sensitivity, not only whether they increase a raw qualifying count; do not use post-hoc thresholds to make failed captures look better. |
| The B1 implementation brief gave a phase-exit instruction that contradicted the governing workflow. | The brief instructed the agent to commit but not push, despite `docs/WORKFLOW.md` and `AGENTS.md` requiring a push at implementation-phase exit. | Baseline reconciliation before B1 edits compared the brief with the higher-priority repository procedure. | The project owner withdrew the no-push instruction and explicitly restored the governing commit-and-push workflow before implementation resumed. | A task brief must reconcile its exit instructions with the governing workflow; stop and resolve any conflict before editing. |
| B1 attempted to prevent Mercury-style wrong-entity resolution with a lexical company-name/domain rule. | The implementation introduced a stricter heuristic than the approved design, which could reject legitimate companies with acronym or non-obvious domains. | ChatGPT post-push inspection of the actual resolver found `domainIsConsistentWithName()` and the passing Mercury fixture depended on it. | The lexical gate was removed; name resolution now requires explicit unambiguous identity evidence, and semantic Mercury behavior remains subject to the bounded live gate. | Wrong-entity safeguards must not recreate V1 by trading silent false positives for broad deterministic false negatives. |
| B1 live identity evaluation was blocked before identity parsing. | The inherited Exa raw-result parser treated every result title as mandatory even though the provider SDK models title as optional and a real response returned an untitled result. | The first Mercury request stopped on `results[9].title`; current official Exa SDK documentation confirmed `SearchResult.title` is nullable. | Title is now optional discovery metadata while strict URL validation remains; the bounded Mercury rerun then reached identity evaluation. | Provider parsers must distinguish required identity fields from optional presentation metadata so an over-strict display field cannot block usable evidence. |
| B2's live clarification could not be diagnosed from its single authorized response. | The design correctly quarantined unresolved production candidates but did not separately require sanitized smoke-harness observability for parsed identity, grounding, B1 decision, and aggregate metadata. | Stripe safely returned `clarification_needed` / `insufficient_identity_evidence`, but the report could not distinguish missing ambiguity, ambiguity, invalid identity fields, grounding/corroboration failure, or provider variability. | B2R0 added transient diagnostic-only smoke observation without changing the production clarification response or retaining raw data. | Fail-closed production boundaries and experiment observability must be designed separately. |

## Candidate-entry rule

Add an entry only when all of these are true:

1. a real problem occurred;
2. AI materially contributed to the wrong assumption, implementation, diagnosis, or recommendation;
3. the problem was detected through review, testing, source inspection, deployment, or another concrete check;
4. a correction or decision followed;
5. there is a useful lesson for the final reflection.

Examples that would qualify if they actually occur:
- hallucinated provider capability;
- incorrect free-tier assumption;
- wrong company resolution;
- fabricated/unsupported source;
- duplicate event selection;
- unnecessary dependency;
- broken deployment recommendation;
- provider rate-limit assumption;
- incorrect bug diagnosis;
- code that fails required TypeScript/build verification.

Do not log ordinary iteration that did not constitute a meaningful mistake.
